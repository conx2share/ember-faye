import fayeExtensions from 'dummy/utils/faye-extensions';
import { module, test } from 'qunit';

module('Unit | Utility | faye extensions');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fayeExtensions;
  assert.ok(result);
});
